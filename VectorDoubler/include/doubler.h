#ifndef DOUBLER_H
#define DOUBLER_H

#include <vector>

// TODO: Declare a function that doubles every element in a vector of ints.
//  - Should take a reference parameter so it modifies the original vector.
void doubleValues(vector,int>& nums);

// TODO: Declare a function that prints the contents of a vector.
//  - Should take a const reference parameter since it only reads data.
void printVector(const vector<int>& nums);

#endif // DOUBLER_H
